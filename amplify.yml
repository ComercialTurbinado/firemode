version: 1
frontend:
  phases:
    preBuild:
      commands:
        - mkdir -p dist
        - cp lp/landing-radar-instagram.html dist/index.html
        - cp lp/protocolo-de-performance.html dist/protocolo-de-performance.html
        - mkdir -p dist/signal && cp lp/signal.html dist/signal/index.html
    build:
      commands:
        - echo "Static build complete"
  artifacts:
    baseDirectory: dist
    files:
      - '**/*'
  cache:
    paths: []
